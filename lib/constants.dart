import 'package:flutter/material.dart';

Color kPrimaryColor = const Color.fromRGBO(199, 147, 232, 1);
Color kBackgroundColor = const Color(0xFFF6FCF9);
TextStyle kTextStyleWhite(weight, size) {
  return TextStyle(
    color: Colors.white,
    fontWeight: weight,
    fontSize: size,
  );
}

TextStyle kTextStyleBlack(weight, size) {
  return TextStyle(
    color: Colors.black,
    fontWeight: weight,
    fontSize: size,
  );
}
